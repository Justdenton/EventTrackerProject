import { RecurringTransaction } from './recurring-transaction';

describe('RecurringTransaction', () => {
  it('should create an instance', () => {
    expect(new RecurringTransaction()).toBeTruthy();
  });
});
