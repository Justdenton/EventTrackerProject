import { SelectedUsernamePipe } from './selected-username.pipe';

describe('SelectedUsernamePipe', () => {
  it('create an instance', () => {
    const pipe = new SelectedUsernamePipe();
    expect(pipe).toBeTruthy();
  });
});
